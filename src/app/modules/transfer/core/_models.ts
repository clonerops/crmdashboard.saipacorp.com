export interface BLandsByStatus {
    fromDate: string
    toDate: string
    contractor_no: number
}
export interface BLandsCarsByStatus {
    fromDate: string
    toDate: string
    dealer_no: number
    contractor_no: number
}